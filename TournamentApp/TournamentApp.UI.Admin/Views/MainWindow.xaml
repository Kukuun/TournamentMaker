<Window x:Class="TournamentApp.UI.Admin.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TournamentApp.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView Width="200" Height="200" ItemsSource="{Binding Players}"
                  SelectedItem="{Binding SelectedPlayer}"
                  DisplayMemberPath="FirstName"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Label Width="100" Height="50" 
               Content="{Binding SelectedPlayer.FirstName, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
               Margin="10"/>
            <Label Width="100" Height="50" 
               Content="{Binding SelectedPlayer.Alias, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
               Margin="10"/>
            <Label Width="100" Height="50" 
               Content="{Binding SelectedPlayer.LastName, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
               Margin="10"/>
        </StackPanel>
        
    </Grid>
</Window>